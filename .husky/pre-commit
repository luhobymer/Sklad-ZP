# Запускаємо lint-staged перед комітом
echo "🔍 Перевірка коду..."
npx lint-staged

if [ $? -ne 0 ]; then
  echo "❌ Перевірка не пройшла. Виправте помилки перед комітом."
  exit 1
fi

echo "✅ Перевірка успішно пройдена!"
